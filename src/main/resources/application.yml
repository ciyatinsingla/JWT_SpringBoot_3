# Data source properties
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jwt_springboot_3
    username: root
    password: password
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 3156
  servlet:
    context-path: /jwtsb3
